# Example environment configuration for the Mingle API

# MongoDB connection string (example – adjust host if needed)
MONGO_URI=mongodb://localhost:27017/mingle

# Example JWT secret (placeholder only)
JWT_SECRET=dpopa04

# Port for the API (optional – defaults to 3000 in app.js)
PORT=3000
